<section class="page-content">
  <div class="page-width page-width--narrow">
    <h1 class="page-content__title">{{ page.title }}</h1>
    <div class="page-content__body">{{ page.content }}</div>
  </div>
</section>

<style>
.page-content { padding: 100px 0 80px; }
.page-content__title { font-size: 2.5rem; font-weight: 300; margin-bottom: 32px; text-align: center; }
.page-content__body { font-size: 1rem; line-height: 1.8; color: var(--color-text-soft); }
.page-content__body h2 { font-size: 1.5rem; margin: 32px 0 16px; color: var(--color-text); }
.page-content__body p { margin-bottom: 16px; }
.page-content__body ul, .page-content__body ol { margin: 16px 0; padding-left: 24px; }
.page-content__body li { margin-bottom: 8px; }
.page-content__body a { color: var(--color-primary); text-decoration: underline; }
</style>

{% schema %}
{ "name": "Page Content", "settings": [] }
{% endschema %}
